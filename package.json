{
  "name": "diffnator",
  "version": "1.0.0",
  "description": "🔥 DiffNator - The ultimate visual website comparison tool with live iframe, proxy, and screenshot modes. Perfect for visual regression testing and pixel-perfect comparisons.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "dev:visible": "cross-env HEADLESS=false node server.js",
    "stop": "node -e \"require('child_process').exec(process.platform === 'win32' ? 'taskkill /F /IM node.exe /FI \\\"WINDOWTITLE eq DiffNator*\\\"' : 'pkill -f \\'node.*server.js\\' || true', () => {})\"",
    "restart": "npm run stop && node -e \"setTimeout(() => {}, 2000)\" && npm start",
    "test": "echo 'No tests yet. Run npm start and test manually at http://localhost:8086'",
    "health": "node -e \"require('http').get('http://localhost:8086/health', res => { let d=''; res.on('data', c => d+=c); res.on('end', () => console.log(d)); }).on('error', e => console.error(e));\""
  },
  "keywords": [
    "visual-testing",
    "visual-regression",
    "diff",
    "pixel-perfect",
    "comparison-tool",
    "screenshot",
    "puppeteer",
    "iframe",
    "proxy",
    "web-development",
    "qa-tools",
    "responsive-design"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Softinator-TechLabs/diffnator.git"
  },
  "bugs": {
    "url": "https://github.com/Softinator-TechLabs/diffnator/issues"
  },
  "homepage": "https://github.com/Softinator-TechLabs/diffnator#readme",
  "author": "Softinator TechLabs",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "express": "^4.21.1",
    "puppeteer": "^24.15.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}

